# {{ project_name }} — E2E tests

Scaffolded by **Aitomation** from a CoverageInventory (source: `{{ source }}`, generated {{ generated_at }}).
Human-authoritative: review everything before relying on it. Nothing here was auto-merged.

- Base URL: `{{ base_url }}`
- Auth strategy: `{{ auth_strategy }}`
- Discovered: {{ pages | length }} page(s), {{ endpoints | length }} endpoint(s){% if has_db %}, {{ tables | length }} table(s){% endif %}{% if has_events %}, {{ topics | length }} topic(s), {{ event_schemas | length }} event schema(s){% endif %}, {{ journeys | length }} suggested journey(s)

## Run

```bash
uv sync
uv run playwright install chromium
BASE_URL={{ base_url }} uv run pytest
```
{% if auth_strategy != "none" %}
Auth is `{{ auth_strategy }}` — set the relevant env vars (see `conftest.py`).
{% endif %}

## Layout

- `conftest.py` — fixtures (base_url, auth{% if has_api %}, API request context{% endif %}{% if has_db %}, db_inspector{% endif %}{% if has_events %}, message_schema{% endif %})
{% if has_browser %}- `pages/` — page objects, one module per discovered page (`base_page.py` is the shared parent;
  import from the package root: `from pages import SomePage`)
{% endif %}{% if has_api %}- `api/` — `ApiClient` seeded from discovered endpoints (`from api import ApiClient`)
{% endif %}{% if has_events %}- `schemas.json` — discovered message payload JSON Schemas (used by `message_schema`)
{% endif %}- `tests/` — `test_smoke.py` at the root; drafted journeys land in `tests/web/`, `tests/api/`,
  and `tests/contract/` by the surface they exercise
- `support/` — reporting hook interface (triage is a separate product) and shared helpers
{% if has_db or has_events %}

## Backend contract tests

Drafts for backend surfaces are **contract-level and read-only** by design (no broker spin-up,
no async round-trip):
{% if has_db %}- **Database** — `db_inspector` reflects the live schema; tests assert tables, columns,
  and constraints. Set `DATABASE_URL` (tests skip without it).
{% endif %}{% if has_events %}- **Events** — `message_schema("<Name>")` returns a discovered JSON Schema; tests
  `jsonschema.validate(...)` a sample payload against it. No running broker required.
{% endif %}{% endif %}

## Suggested journeys to cover (from discovery)
{% for j in journeys %}
- **{{ j.name }}** — {{ j.description }}
{% endfor %}
