"""Shared base for the generated page objects of {{ project_name }}.

Generated by Aitomation Scaffold. Page objects hold locators and actions; tests hold
assertions — keep that boundary when extending these classes.
"""

from typing import Self

from playwright.sync_api import Page


class BasePage:
    """Construction with a live `page` plus navigation to the page's PATH."""

    __test__ = False  # a page object must never be collected by pytest

    PATH = "/"

    def __init__(self, page: Page) -> None:
        self.page = page

    def goto(self) -> Self:
        # Relative navigation — pytest-playwright resolves it against the configured base_url.
        self.page.goto(self.PATH)
        return self
